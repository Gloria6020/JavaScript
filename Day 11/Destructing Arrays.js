const numbers = [1, 2, 3, ]
let[numOne, numTwo, numThree, ] = numbers
console.log(numbers)
console.log(numOne, numTwo, numThree, )



const names = ['Gloria', 'Cherop']
let[firstName, secondName, ] = names

console.log(firstName, secondName, )